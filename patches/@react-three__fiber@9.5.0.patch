diff --git a/dist/events-238e0986.cjs.prod.js b/dist/events-238e0986.cjs.prod.js
index 2204a1acee6fe96fd716f28c9f4b4860d61f2d98..df79960d716f2f2a4b32ee938d78bfdc1c35564d 100644
--- a/dist/events-238e0986.cjs.prod.js
+++ b/dist/events-238e0986.cjs.prod.js
@@ -122,6 +122,44 @@ function calculateDpr(dpr) {
   const target = typeof window !== 'undefined' ? (_window$devicePixelRa = window.devicePixelRatio) != null ? _window$devicePixelRa : 2 : 1;
   return Array.isArray(dpr) ? Math.min(Math.max(dpr[0], target), dpr[1]) : dpr;
 }
+function createCompatClock() {
+  return {
+    autoStart: true,
+    startTime: 0,
+    oldTime: 0,
+    elapsedTime: 0,
+    running: false,
+    start() {
+      this.startTime = performance.now();
+      this.oldTime = this.startTime;
+      this.elapsedTime = 0;
+      this.running = true;
+    },
+    stop() {
+      this.getElapsedTime();
+      this.running = false;
+      this.autoStart = false;
+    },
+    getElapsedTime() {
+      this.getDelta();
+      return this.elapsedTime;
+    },
+    getDelta() {
+      let diff = 0;
+      if (this.autoStart && !this.running) {
+        this.start();
+        return 0;
+      }
+      if (this.running) {
+        const newTime = performance.now();
+        diff = (newTime - this.oldTime) / 1000;
+        this.oldTime = newTime;
+        this.elapsedTime += diff;
+      }
+      return diff;
+    }
+  };
+}
 
 /**
  * Returns instance root state
@@ -994,7 +1032,7 @@ const createStore = (invalidate, advance) => {
       linear: false,
       flat: false,
       controls: null,
-      clock: new THREE__namespace.Clock(),
+      clock: createCompatClock(),
       pointer,
       mouse: pointer,
       frameloop: 'always',
diff --git a/dist/events-358c3764.cjs.dev.js b/dist/events-358c3764.cjs.dev.js
index 40f145a71415c3a7fabe631132183d557dc61b22..c1f99393de0ceb2b80be9e8b85319442dffd717e 100644
--- a/dist/events-358c3764.cjs.dev.js
+++ b/dist/events-358c3764.cjs.dev.js
@@ -122,6 +122,44 @@ function calculateDpr(dpr) {
   const target = typeof window !== 'undefined' ? (_window$devicePixelRa = window.devicePixelRatio) != null ? _window$devicePixelRa : 2 : 1;
   return Array.isArray(dpr) ? Math.min(Math.max(dpr[0], target), dpr[1]) : dpr;
 }
+function createCompatClock() {
+  return {
+    autoStart: true,
+    startTime: 0,
+    oldTime: 0,
+    elapsedTime: 0,
+    running: false,
+    start() {
+      this.startTime = performance.now();
+      this.oldTime = this.startTime;
+      this.elapsedTime = 0;
+      this.running = true;
+    },
+    stop() {
+      this.getElapsedTime();
+      this.running = false;
+      this.autoStart = false;
+    },
+    getElapsedTime() {
+      this.getDelta();
+      return this.elapsedTime;
+    },
+    getDelta() {
+      let diff = 0;
+      if (this.autoStart && !this.running) {
+        this.start();
+        return 0;
+      }
+      if (this.running) {
+        const newTime = performance.now();
+        diff = (newTime - this.oldTime) / 1000;
+        this.oldTime = newTime;
+        this.elapsedTime += diff;
+      }
+      return diff;
+    }
+  };
+}
 
 /**
  * Returns instance root state
@@ -994,7 +1032,7 @@ const createStore = (invalidate, advance) => {
       linear: false,
       flat: false,
       controls: null,
-      clock: new THREE__namespace.Clock(),
+      clock: createCompatClock(),
       pointer,
       mouse: pointer,
       frameloop: 'always',
diff --git a/dist/events-5a94e5eb.esm.js b/dist/events-5a94e5eb.esm.js
index e907a7f47bed09693521adbf50d8966c3bb660af..4cf3b934ddd6ec47ad042b00a9ff667b2a0ff5b4 100644
--- a/dist/events-5a94e5eb.esm.js
+++ b/dist/events-5a94e5eb.esm.js
@@ -97,6 +97,44 @@ function calculateDpr(dpr) {
   const target = typeof window !== 'undefined' ? (_window$devicePixelRa = window.devicePixelRatio) != null ? _window$devicePixelRa : 2 : 1;
   return Array.isArray(dpr) ? Math.min(Math.max(dpr[0], target), dpr[1]) : dpr;
 }
+function createCompatClock() {
+  return {
+    autoStart: true,
+    startTime: 0,
+    oldTime: 0,
+    elapsedTime: 0,
+    running: false,
+    start() {
+      this.startTime = performance.now();
+      this.oldTime = this.startTime;
+      this.elapsedTime = 0;
+      this.running = true;
+    },
+    stop() {
+      this.getElapsedTime();
+      this.running = false;
+      this.autoStart = false;
+    },
+    getElapsedTime() {
+      this.getDelta();
+      return this.elapsedTime;
+    },
+    getDelta() {
+      let diff = 0;
+      if (this.autoStart && !this.running) {
+        this.start();
+        return 0;
+      }
+      if (this.running) {
+        const newTime = performance.now();
+        diff = (newTime - this.oldTime) / 1000;
+        this.oldTime = newTime;
+        this.elapsedTime += diff;
+      }
+      return diff;
+    }
+  };
+}
 
 /**
  * Returns instance root state
@@ -969,7 +1007,7 @@ const createStore = (invalidate, advance) => {
       linear: false,
       flat: false,
       controls: null,
-      clock: new THREE.Clock(),
+      clock: createCompatClock(),
       pointer,
       mouse: pointer,
       frameloop: 'always',
